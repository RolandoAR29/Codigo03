import { Component } from '@angular/core';

// DECORADOR -> "sobreescribir" en Component"
@Component({
    selector: 'app-header', // Nombre del selector o "etiqueta HTML"
    templateUrl: './header.component.html'
})
export class HeaderComponent{

}